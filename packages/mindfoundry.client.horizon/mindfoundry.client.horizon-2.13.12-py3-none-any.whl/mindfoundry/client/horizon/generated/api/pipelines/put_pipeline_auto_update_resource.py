from typing import Any, Dict

import httpx

from ...client import Client
from ...models.bool_auto_update import BoolAutoUpdate
from ...types import Response


def _get_kwargs(
    *,
    client: Client,
    id: int,
    json_body: BoolAutoUpdate,
) -> Dict[str, Any]:
    url = "{}/pipelines/{id}/auto_update".format(client.base_url, id=id)

    headers: Dict[str, Any] = client.get_headers()
    cookies: Dict[str, Any] = client.get_cookies()

    json_json_body = json_body.to_dict()

    return {
        "url": url,
        "headers": headers,
        "cookies": cookies,
        "timeout": client.get_timeout(),
        "json": json_json_body,
    }


def _build_response(*, response: httpx.Response) -> Response[Any]:
    return Response(
        status_code=response.status_code,
        content=response.content,
        headers=response.headers,
        parsed=None,
    )


def sync_detailed(
    *,
    client: Client,
    id: int,
    json_body: BoolAutoUpdate,
) -> Response[Any]:
    kwargs = _get_kwargs(
        client=client,
        id=id,
        json_body=json_body,
    )

    response = httpx.put(
        **kwargs,
    )

    return _build_response(response=response)


async def asyncio_detailed(
    *,
    client: Client,
    id: int,
    json_body: BoolAutoUpdate,
) -> Response[Any]:
    kwargs = _get_kwargs(
        client=client,
        id=id,
        json_body=json_body,
    )

    async with httpx.AsyncClient() as _client:
        response = await _client.put(**kwargs)

    return _build_response(response=response)
