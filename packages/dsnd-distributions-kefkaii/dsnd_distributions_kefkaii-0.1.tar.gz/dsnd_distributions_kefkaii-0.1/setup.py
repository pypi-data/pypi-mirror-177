from setuptools import setup

setup(name='dsnd_distributions_kefkaii',
      version='0.1',
      description='Gaussian distributions',
      packages=['dsnd_distributions_kefkaii'],
      zip_safe=False)
