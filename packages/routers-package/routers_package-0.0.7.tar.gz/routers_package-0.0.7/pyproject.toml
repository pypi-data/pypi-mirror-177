[project]
name = "routers_package"
version = "0.0.7"
description = "Package for routing grocom project"
readme = "README.md"
requires-python = "==3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies=[
    'PyJWT'
]
