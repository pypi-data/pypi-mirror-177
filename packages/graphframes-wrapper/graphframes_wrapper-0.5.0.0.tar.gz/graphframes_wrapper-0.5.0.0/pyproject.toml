[tool.poetry]
name = "graphframes-wrapper"
version = "0.5.0.0"
description = ""
authors = ["Anthony Edwards <anthonygedwards93@gmail.com>"]
readme = "README.md"
packages = [{include = "graphframes_wrapper"}]

[tool.poetry.dependencies]
python = "^3.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
