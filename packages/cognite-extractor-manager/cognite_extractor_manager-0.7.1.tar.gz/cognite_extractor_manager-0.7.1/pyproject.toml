[tool.poetry]
name = "cognite-extractor-manager"
version = "0.7.1"
description = "A project manager for Python based extractors"
authors = ["Mathias Lohne <mathias.lohne@cognite.com>"]
license = "Apache-2.0"
readme = "README.md"

packages = [
    { include="cognite", from="." },
]

[tool.black]
line-length = 120
target_version = ['py38']
include = '\.py$'

[tool.isort]
line_length=120                # corresponds to -w  flag
multi_line_output=3            # corresponds to -m  flag
include_trailing_comma=true    # corresponds to -tc flag
skip_glob = '^((?!py$).)*$'    # this makes sort all Python files
known_third_party = ["requests", "termcolor", "toml"]

[tool.poetry.dependencies]
python = "^3.8.0"
termcolor = "^1.1.0"
requests = "^2.26.0"
toml = "^0.10.2"

[tool.poetry.dev-dependencies]
black = "*"
isort = "*"
twine = "^3.8.0"
flake8 = "*"
pre-commit = "^2.2.0"
pyinstaller = "^4.0"

[tool.poetry.scripts]
cogex = "cognite.extractorutils.cogex.__main__:main"

[tool.cogex.docker]
base-image = "python:3.9-bullseye"
tags = [
    "cognite/extractor-manager:latest",
    "cognite/extractor-manager:{version}"
]
preamble = """
# Install docker to build docker images (from https://docs.docker.com/engine/install/debian/)
RUN apt-get update
RUN apt-get install -y ca-certificates curl gnupg lsb-release
RUN curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
RUN echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/debian $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
RUN apt-get update
RUN apt-get install -y docker-ce docker-ce-cli containerd.io

# Install libs and compilers used by python libs
RUN apt-get install -y unixodbc-dev
"""
install-dir = "/cogex"
work-dir = "/"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
