[tool.poetry]
name = "http_client_sync"
version = "0.0.6b"
description = "Http Client for work with API."
authors = ["Alexandr Kolesnikov"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.28.1"
beautifulsoup4 = "^4.11.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
