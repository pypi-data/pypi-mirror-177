[metadata]
author = "Transpara"
author_email = "arturo.aguilar@transpara.com"

[project]
name = "transpara-py-sdk"
version = "0.0.1"
description = "Python SDK"

[project.urls]
homepage = "https://transpara.com"

[options]
package_dir = "src"
python_requires = ">=3.6"

[tool.pytest.ini_options]
pythonpath = [
  ".", "src"
]

[build-system]
requires = ['setuptools>=42']
build-backend = 'setuptools.build_meta'