[tool.poetry]
name = "pxycrypto"
version = "0.1.0"
description = ""
authors = ["Z1R343L"]
packages = [
    { include = "pxycrypto" },
    { include = "pxycrypto/**/*.py" },
]
include = ["pxycrypto/assets/*.json"]

[tool.poetry.dependencies]
python = "^3.11"


[tool.poetry.group.dev.dependencies]
isort = "^5.10.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
