[build-system]
requires = [
    "setuptools>=30.3.0",
    "setuptools_scm>=6.2",
    "wheel",
    ]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "pycraf_gui/version.py"

# [tool.pytest.ini_options]
# minversion = "6.0"
# addopts = '-rsx --doctest-rst  --doctest-modules --ignore-glob="*/setup_package.py"'
# testpaths = [
#     "pycraf",
#     "docs",
# ]
