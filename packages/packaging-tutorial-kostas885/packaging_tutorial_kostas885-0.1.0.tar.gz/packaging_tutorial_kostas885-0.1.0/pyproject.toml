[tool.poetry]
name = "packaging-tutorial-kostas885"
version = "0.1.0"
description = ""
authors = ["developers <kostas@alumnireach.com>"]
readme = "README.md"
packages = [{include = "packaging_tutorial"}]

[tool.poetry.dependencies]
python = "^3.9"
flask = "^2.2.2"


[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
