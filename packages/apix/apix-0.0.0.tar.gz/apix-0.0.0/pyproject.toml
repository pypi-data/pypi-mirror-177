[tool.poetry]
name = "apix"
version = "0.0.0"
description = ""
authors = ["Arwichok <me@arwi.dev>"]

[tool.poetry.dependencies]
python = "^3.8"
httpx = "^0.23.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[project.scripts]
apix = "apix.cli:run"
