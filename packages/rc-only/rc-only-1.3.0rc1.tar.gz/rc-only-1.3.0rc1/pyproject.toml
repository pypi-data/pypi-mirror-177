[build-system]
requires = ["setuptools >= 40.6.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rc-only"
version = "1.3.0rc1"
description = 'foo'
classifiers = [
  "Programming Language :: Python :: 3.10"
]
